<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 	
    <mapper namespace="com.test_mybatis">	//表示命名空间是com.test_mybatis，在后续调用sql语句的时候，会用到它
        <select id="listCategory" resultType="Category">	//listCategory用于表示sql语句供后续代码调用，resultType="Category"表示返回的数据和Category关联起来
            select * from   category_      
        </select>
    </mapper>